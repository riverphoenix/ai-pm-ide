{
  "id": "rice",
  "name": "RICE Prioritization",
  "description": "Prioritize features using Reach, Impact, Confidence, Effort",
  "category": "prioritization",
  "icon": "📊",
  "fields": [
    {
      "id": "feature_name",
      "label": "Feature Name",
      "type": "text",
      "required": true,
      "placeholder": "e.g., Dark mode toggle",
      "ai_assist_prompt": "Suggest a clear, concise feature name based on context"
    },
    {
      "id": "reach",
      "label": "Reach (users/month)",
      "type": "number",
      "required": true,
      "placeholder": "e.g., 10000",
      "ai_assist_prompt": "Estimate monthly user reach based on target segment and available data"
    },
    {
      "id": "impact",
      "label": "Impact",
      "type": "select",
      "options": [
        { "value": "0.25", "label": "Minimal (0.25)" },
        { "value": "0.5", "label": "Low (0.5)" },
        { "value": "1", "label": "Medium (1)" },
        { "value": "2", "label": "High (2)" },
        { "value": "3", "label": "Massive (3)" }
      ],
      "required": true,
      "ai_assist_prompt": "Assess impact level (0.25-3) based on user pain points and strategic importance"
    },
    {
      "id": "confidence",
      "label": "Confidence (%)",
      "type": "number",
      "required": true,
      "placeholder": "e.g., 80",
      "ai_assist_prompt": "Rate confidence (0-100%) in the reach and impact estimates"
    },
    {
      "id": "effort",
      "label": "Effort (person-months)",
      "type": "number",
      "required": true,
      "placeholder": "e.g., 2",
      "ai_assist_prompt": "Estimate engineering effort in person-months"
    },
    {
      "id": "rationale",
      "label": "Rationale",
      "type": "textarea",
      "required": false,
      "placeholder": "Explain your reasoning...",
      "ai_assist_prompt": "Provide rationale for this prioritization decision based on the scores"
    }
  ],
  "computed_fields": [
    {
      "id": "rice_score",
      "formula": "(reach * impact * confidence / 100) / effort"
    }
  ],
  "output_template": "# {{feature_name}}\n\n## RICE Score: {{rice_score}}\n\n### Scoring\n- **Reach**: {{reach}} users/month\n- **Impact**: {{impact}}\n- **Confidence**: {{confidence}}%\n- **Effort**: {{effort}} person-months\n\n### Rationale\n{{rationale}}"
}
